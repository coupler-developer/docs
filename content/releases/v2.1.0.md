# 2.1.0 릴리스 실행 기록

## 문서 역할

- 역할: `시나리오`
- 문서 종류: `flow`
- 충돌 시 우선 문서: `policy/release-process.md`
- 기준 성격: `as-is`

## 목적

- `v2.1.0` 릴리스의 서버 운영 반영 결과, 모바일 스토어 제출 상태, 태그 기준점을 한 문서에 고정한다.

## 범위

- 대상: `docs`, `coupler-api`, `coupler-admin-web`, `coupler-mobile-app`
- 포함 범위: API/Admin 운영 배포, 서비스 태그 확정, Mobile Store 제출 상태 기록, docs Release 기록
- 제외 범위: 운영 `RDS` 변경, Mobile NextPush 배포, 스토어 심사 정책 해석

## 상위 규범 문서

- [배포 태그/릴리즈 프로세스](../policy/release-process.md)
- [테스트/CI 전략](../policy/testing-strategy.md)
- [엔지니어링 가드레일](../policy/engineering-guardrails.md)

## 릴리스 상태

- 목표 버전: `v2.1.0`
- 전체 상태: `in_progress`
- 완료 범위: `coupler-api`, `coupler-admin-web`, `docs release record`
- 대기 범위: `coupler-mobile-app` Mobile Store 승인 및 운영 출시 검증
- `coupler-api` 태그: `v2.1.0` -> `09dca9955cb9bbe49cb9a49584afc6ef8be627ec`
- `coupler-admin-web` 태그: `v2.1.0` -> `ea5e017fc92cf446e6a7df49e7c7fc975fe6817d`
- `docs` 태그: `v2.1.0` -> release workflow의 현재 docs tag commit
- `coupler-mobile-app` 태그: 대기. 스토어 승인 후 운영 출시와 기본 검증이 끝난 커밋에 생성한다.

## 릴리스 결과

- `coupler-api` 운영 EC2를 `09dca9955cb9bbe49cb9a49584afc6ef8be627ec` 기준으로 배포하고 외부 응답을 확인했다.
- `coupler-admin-web` 운영 정적 산출물을 `ea5e017fc92cf446e6a7df49e7c7fc975fe6817d` 기준으로 배포하고 외부 응답을 확인했다.
- Android/iOS 스토어 빌드는 `2.1.0 (90)`으로 제출했다.
- Mobile Store가 아직 운영 출시/검증 전이므로 `coupler-mobile-app` `v2.1.0` 태그는 생성하지 않는다.
- 이번 릴리스에서 운영 `RDS` 변경과 Mobile NextPush 배포는 포함하지 않는다.

## 메인 흐름

1. 개발계 API/Admin 배포를 완료한 뒤 운영계 API/Admin 배포 범위를 확정했다.
2. 운영 `coupler-api`에서 최신 `main` 반영, 의존성 설치, PM2 restart/save를 수행했다.
3. 운영 `coupler-admin-web`에서 최신 `main` 반영, production build, 정적 산출물 교체, nginx 검증/재시작을 수행했다.
4. Mobile Android AAB와 iOS archive를 `2.1.0 (90)`으로 스토어 심사에 제출했다.
5. API/Admin 운영 검증이 완료된 커밋에 서비스 태그를 생성하고, docs 릴리스 기록으로 완료/대기 범위를 분리했다.

## 검증 근거

- `coupler-api`
    - 운영 커밋: `09dca9955cb9bbe49cb9a49584afc6ef8be627ec`
    - PM2 상태: `coupler-api` online
    - 내부 응답: `http://127.0.0.1:3002/` 200
    - 외부 응답: `https://api.ritzy.fourhundred.co.kr/` 200
- `coupler-admin-web`
    - 운영 커밋: `ea5e017fc92cf446e6a7df49e7c7fc975fe6817d`
    - build artifact: `main.92cea922.js`
    - 운영 백업: `/var/www/coupler-admin-web-backup-20260613222006`
    - nginx 검증: `nginx -t` success
    - 내부 응답: `http://127.0.0.1:8000` 200
    - 외부 응답: `https://cms.ritzy.fourhundred.co.kr` 200
- `coupler-mobile-app`
    - Android 제출 빌드: `2.1.0 (90)`, 기준 커밋 `d15a25de0909779b7ee4f82b9f4353a7a45c4ed2`
    - iOS 제출 빌드: `2.1.0 (90)`, 기준 커밋 `cc196b24c8d92062af3c76a57b760fca178044fa`
    - 제출 후 iOS build tooling/fmt 정리 커밋: `956481a2b1c11d34a32bdf369e1a555761f5c72d`, `5693c060368fda23bcadf0cd146090a1f3388fcc`

## 롤백 기준

- `coupler-api`: 직전 운영 태그 또는 운영 배포 전 PM2 상태 기준으로 롤백한다.
- `coupler-admin-web`: `/var/www/coupler-admin-web-backup-20260613222006` 백업 산출물 또는 직전 운영 태그 기준으로 롤백한다.
- `coupler-mobile-app`: 스토어 승인 전에는 운영 사용자에게 반영되지 않았으므로 스토어 콘솔에서 제출 빌드 상태를 기준으로 처리한다.
- 운영 `RDS`: 이번 릴리스에 DB 변경이 없으므로 DB rollback 대상은 `N/A`다.

## 후속 작업

- Mobile Store 심사 통과 후 실제 운영 출시와 기본 smoke 검증을 완료한다.
- 검증 완료 커밋에 `coupler-mobile-app` `v2.1.0` annotated tag를 생성한다.
- 필요한 경우 이 문서와 docs GitHub Release 본문을 `released` 상태로 갱신한다.

## 관련 문서

- [배포 태그/릴리즈 프로세스](../policy/release-process.md)
- [운영 배포 명령어 런북](../flows/cross-project/production-deploy-command-runbook.md)
- [레포지토리 요약](../architecture/repo-overview.md)
