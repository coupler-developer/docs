# 2.5.3 릴리스 실행 기록

```release-metadata
{
  "schema": "release-metadata/v3",
  "version": "v2.5.3",
  "status": "planned",
  "releaseScopes": [
    "db-migration",
    "coupler-api",
    "coupler-admin-web",
    "mobile-store",
    "docs"
  ],
  "extraRepoRefs": [],
  "versionMapping": {
    "docs": {
      "tag": null,
      "commit": null
    },
    "coupler-api": {
      "tag": null,
      "commit": "a089ecd007850004cb3e6ee21224b9ad4e450ba0"
    },
    "coupler-admin-web": {
      "tag": null,
      "commit": "475e9b2dcafb2834420688869aca9d348fcf4114"
    },
    "coupler-mobile-app": {
      "store": {
        "android": {
          "versionBuild": "2.5.0 (105)",
          "releaseTag": null,
          "commit": "d53fe50c49077b03a98ed6be9fd20f75152752d3",
          "sourceStatus": "verified",
          "limitation": null
        },
        "ios": {
          "versionBuild": "2.5.0 (105)",
          "releaseTag": null,
          "commit": "d53fe50c49077b03a98ed6be9fd20f75152752d3",
          "sourceStatus": "verified",
          "limitation": null
        }
      },
      "nextPush": null,
      "commit": null
    }
  },
  "scopeResults": {
    "db-migration": {
      "status": "planned",
      "summary": "API source a089ecd0에 포함된 N:N 그룹미팅 강제삭제 migration 1건만 개발계와 운영계에 같은 exact source로 적용한다.",
      "evidence": {}
    },
    "coupler-api": {
      "status": "planned",
      "summary": "URL 기능을 제외한 prior-main source a089ecd0을 후보로 고정했으며, 운영 실행 전 origin/main 동등성 Deploy·Tag Gate를 해소해야 한다.",
      "evidence": {
        "deployment": null,
        "smoke": null,
        "publicContract": null,
        "runtimeRecovery": null
      }
    },
    "coupler-admin-web": {
      "status": "planned",
      "summary": "현재 main source 475e9b2d의 N:N 강제삭제 관리 UI를 운영 정적 artifact로 배포할 계획이다.",
      "evidence": {
        "deployment": null,
        "smoke": null,
        "rollback": null
      }
    },
    "mobile-store": {
      "status": "planned",
      "summary": "Mobile PR #240의 2.5.0 (105) 후보 d53fe50c는 품질 Gate를 통과했으며 승인·main 병합과 Store artifact 생성을 기다린다.",
      "evidence": {
        "submission": null,
        "approval": null,
        "release": null,
        "smoke": null,
        "artifact": null,
        "submittedMarkers": {
          "android": null,
          "ios": null
        }
      }
    },
    "docs": {
      "status": "planned",
      "summary": "DB/API/Admin/Mobile Store 결과를 같은 Draft PR에 누적하고 terminal 상태에서 최종 릴리스 기록과 docs tag를 준비한다.",
      "evidence": {}
    }
  },
  "apiContractCutover": null
}
```

## 문서 역할

- 역할: `시나리오`
- 문서 종류: `flow`
- 충돌 시 우선 문서: `policy/release-process.md`, 태그 기준은 `policy/release-tag-policy.md`
- 기준 성격: `as-is`

## 목적

- `v2.5.3` 릴리스의 실제 반영 결과와 검증 근거를 한 문서에 고정한다.

## 범위

- 대상: `docs`, `coupler-api`, `coupler-admin-web`, `coupler-mobile-app`
- 포함 범위: N:N 그룹미팅 강제삭제 migration 1건, API·Admin 운영 배포, Mobile Store `2.5.0 (105)`, 릴리스 기록
- 제외 범위: 매니저 상세 프로필 URL migration·API·Admin·Mobile 변경, contracts package 재발행, Mobile NextPush

## 상위 규범 문서

- [릴리스 프로세스](../policy/release-process.md)
- [릴리스 태그 정책](../policy/release-tag-policy.md)
- [테스트/CI 전략](../policy/testing-strategy.md)

## 릴리스 상태

- 목표 버전: `v2.5.3`
- 전체 상태: `planned`
- 완료 범위: Mobile `2.5.0 (105)` 후보의 로컬·PR 품질 Gate
- 대기 범위: Mobile PR #240 승인·main 병합, 릴리스 preflight, DB/API/Admin 운영 반영, Store 제출·승인·출시, 서비스·docs 태그

## 버전 매핑

- `docs`: 기록 버전 `v2.5.3`, 태그 `N/A`, 커밋 `N/A`
- `coupler-api`: 태그 `N/A`, 커밋 `a089ecd007850004cb3e6ee21224b9ad4e450ba0`
- `coupler-admin-web`: 태그 `N/A`, 커밋 `475e9b2dcafb2834420688869aca9d348fcf4114`
- `coupler-mobile-app`: Android Store `2.5.0 (105)`, Android 릴리스 태그 `N/A`, Android 커밋 `d53fe50c49077b03a98ed6be9fd20f75152752d3`, Android source `verified`, iOS Store `2.5.0 (105)`, iOS 릴리스 태그 `N/A`, iOS 커밋 `d53fe50c49077b03a98ed6be9fd20f75152752d3`, iOS source `verified`, NextPush `N/A`, NextPush 커밋 `N/A`
- Store 제출 마커 태그와 증빙 이관/삭제: `N/A` - artifact 생성·제출 전

## 작성 기준

- 신규 기록은 템플릿을 직접 복사하지 않고 docs 레포 루트에서 `yarn release:record:init v2.5.3`로 초기화해
  lifecycle registry, `content/AGENTS.md` 인덱스와 `mkdocs.yml` nav를 같은 변경 단위로 준비한다.
- 이 파일은 입력 골격이다. 상태·scope·evidence의 규범과 closed value는
  [릴리스 프로세스](../policy/release-process.md)의 `릴리스 운영 모델`과 `릴리스 기록 상태값`을 따른다.
- 위 `release-metadata`는 docs-only `planned` 시작형이다. 실제 `releaseScopes`와 같은 key만 `scopeResults`에
  추가하고, `scripts/release-schema.mjs`가 정의하지 않은 key나 별도 완료 축을 만들지 않는다.
- scope, exact commit, 검증 시나리오와 rollback 기준을 확정하고 현재 PR head에 적용된 필수 CI를 확인한 뒤
  전체 상태와 nonterminal `scopeResults`를 `pending`으로 전환한다. 전환을 push한 현재 PR head의 필수 CI
  성공을 다시 확인하기 전에는 preflight나 운영 실행을 시작하지 않는다.
- `planned | pending | in_progress` 동안 PR은 Draft로 유지한다. 모든 scope와 전체 상태를 terminal로 닫고
  독립 리뷰·최종 검증을 통과한 뒤에만 Ready로 전환해 병합한다.
- `release-metadata`가 기계 판정 SoT이고 본문은 사람이 읽는 mirror다. 두 표현의 범위·상태·버전 기준을 맞춘다.
- terminal scope는 정책과 descriptor가 요구하는 concrete evidence로 닫는다. placeholder나 `N/A`로 완료
  증빙을 대신하지 않는다.
- API contract cutover가 있을 때만 `content/templates/api-contract-cutover-gate-template.md`를
  `검증 근거` 아래에 삽입한다.
- DB migration은 [DB Migration 실행 런북](../flows/cross-project/db-migration-operation-flow.md)에 따라 개발계와
  운영계에 사용한 동일한 마이그레이션 소스 커밋과 최종 pending 0건을 사람이 읽는 결과에 기록한다. 별도
  plan·execution artifact는 만들지 않는다.
- `main`에 이미 병합된 terminal 릴리스 기록과 evidence는 수정·삭제·개명·대체하거나 현재 계약으로 재검증하지
  않는다. nonterminal 오병합은 릴리스 프로세스의 fail-closed 단일 terminalization만 사용한다.
- 개인 사용자명·로컬 home/tmp 경로·secret을 기록하지 않는다. 변동 값에는 확인 시각과 timezone을 남긴다.
- 최종 후보에서 `yarn release:preflight --pending-ref <40자 commit SHA>`와 적용 docs 품질 Gate를 실행한다.

## 릴리스 결과

- 현재 결과: `planned` - 서비스 후보와 scope를 고정했다. Mobile main 기준점과 릴리스 preflight는 미확정이며, API `a089ecd0`은 현재 `origin/main`이 아니므로 표준 Deploy·Tag Gate가 차단된 상태다.

## 메인 흐름

1. Mobile release PR #240을 승인·병합하고 `origin/main` exact source를 고정한다.
2. API target과 `origin/main`의 표준 Deploy·Tag Gate 불일치를 해소한다. 해소 전에는 `pending` 또는 운영 실행으로 전환하지 않는다.
3. 같은 Draft docs PR에서 상태를 `pending`으로 전환하고 현재 head CI와 공통 preflight를 통과한다.
4. API `a089ecd0` source의 강제삭제 migration 1건만 개발계와 운영계에 순서대로 확인·적용한다.
5. API·Admin 운영 배포와 postcheck를 수행하고 허용된 서비스 태그를 고정한다.
6. Mobile `2.5.0 (105)` platform artifact를 제출하고 승인·출시·smoke와 제출 마커 증빙을 완료한다.
7. 같은 docs PR을 terminal 기록으로 닫아 병합한 뒤 docs 태그·Release·Pages를 postcheck한다.

## 검증 근거

- Preflight 명령·결과: `planned` 상태 - 미실행; API 후보 `a089ecd0`과 현재 `origin/main` 불일치를 해소하기 전에는 pending preflight를 실행하지 않는다.
- Scope별 Gate·smoke·artifact: Mobile PR #240 head `d53fe50c49077b03a98ed6be9fd20f75152752d3`의 lint·format·typecheck·test·Storybook CI 통과
- API cutover: `No` - API `a089ecd0`의 `packages/contracts` tree는 직전 운영 API와 동일한 `0.1.32`이며 legacy lounge `best` wire를 유지한다.
- DB 마이그레이션 소스 커밋·개발계/운영계 결과: source `a089ecd007850004cb3e6ee21224b9ad4e450ba0`; 강제삭제 migration 1건, 운영 미적용, 실행 전
- Workflow·로그·수동 검증 링크: Mobile PR #240, API PR #222, Admin PR #114

### Mobile 개발계 QA 빌드 기록

개발계 QA 빌드가 있을 때만 기록한다. 운영 Store·NextPush·서비스 태그 증빙으로 사용하지 않는다.

- 기록일:
- API 대상:
- iOS TestFlight QA 빌드:
- Android QA APK:
- 운영 릴리스 전 확인:

## 롤백 기준

- DB: migration을 역쿼리하지 않고 ledger와 live schema를 확인한 뒤 forward fix한다.
- API: 직전 운영 API가 강제삭제 migration 이후 schema와 호환된다는 exact evidence가 있을 때만 rollback하고, 없으면 `a089ecd0` 기반의 검증된 forward fix로 복구한다.
- Admin: 배포 직전 live source·index SHA가 봉인된 backup directory로만 원자 복구한다.
- Mobile Store: 제출 전에는 배포 효과가 없으며, 제출 뒤에는 플랫폼 콘솔 상태와 submitted marker 기준으로 중단·재제출을 판단한다.
- docs: terminal 병합 전에는 같은 Draft PR에서 기록을 보정하고 병합·태그 뒤에는 수정하지 않는다.

## 후속 작업

- 남은 범위: Mobile PR 승인·main 병합, API Deploy·Tag Gate 정렬, docs pending Gate, DB/API/Admin 운영 반영, Store 제출·승인·출시, 태그와 postcheck
- 완료 조건: 포함 scope의 실제 증빙과 서비스 태그를 고정하고 terminal docs 기록·Release·Pages postcheck를 완료한다.
